[pytest]
pythonpath =
    .
    finance_intel
testpaths =
    tests
# finance_intel 独立 Docker 化、依赖（bs4 等）自备，其子套件在其容器/目录内
# 单独跑（cd finance_intel && python3 -m pytest tests/）；根环境无 bs4，
# 不纳入根 testpaths（2026-08-06，原配置致根目录裸跑 pytest 2 个 collection error）
markers =
    external_artifacts: 依赖未提交的个人券商/PDF 生成物
